name: flutter_weather
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^0.17.0
  hydrated_bloc: ^0.2.1
  http: ^0.12.0
  equatable: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.3.0
  mockito: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
